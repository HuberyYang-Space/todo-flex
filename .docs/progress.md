# 进度与待办

> 什么时候读：每次开工。它回答「现在做到哪了、下一件是什么、哪些结论已经被真实浏览器验证过」。
> 这里只有叙述，没有禁令——禁令在 [CLAUDE.md](../CLAUDE.md)，禁令背后的论证在 [pitfalls.md](./pitfalls.md)。

## 里程碑状态

| 里程碑 | 状态 | 说明 |
| --- | --- | --- |
| M1 推导引擎 | ✅ 完成 | `resolveBasis` → `splitLines` → `distribute`，由 `deriveLayout` 组装 |
| M2 可用的 Playground | ✅ 完成 | 表驱动控制面板 + 真实 flex 演示区 |
| M3 透明化核心 | ✅ 完成，经浏览器实测 | 观测 / 诊断 / 明细表 / 叠加层 / resize 手柄 |
| M4 动效 | ✅ 实现完成，动效观感待人眼核对 | GSAP Flip、惯性水滴形变、观测层挂起、演示区质感升级；3D 方案已放弃 |
| M6 打磨 | ✅ URL 分享 + 暗亮主题打磨均已完成；i18n 已移出 | 判据由 `theme.spec.ts` 钉住 |
| M7 布局改版 | ✅ 完成，经浏览器实测 | 整页不滚动、操作区内部滚、演示区加宽、明细与 CSS 收进标签面板 |

> ⚠️ `.docs/superpowers/plans/` 里的计划文档 checkbox 全是未勾选状态，但代码与 git 历史证明这些里程碑已实现完毕。
> **判断进度以 git 历史和实际代码为准，不要被 checkbox 误导。**

## M3：透明化核心

全部经浏览器实测。落地的东西：

- 观测层 `composables/useMeasure.ts`
- 诊断层 `core/diagnostics.ts`
- 理论 vs 实际明细表
- SVG 叠加层 `components/playground/OverlayLayer.vue`（剩余空间斜纹色块、溢出标记、轴向箭头、尺寸 HUD）
- 演示区右下角 resize 拖拽手柄 `StageResizer`（已替代原来的两个 range 滑块）

M3 暴露的三个「单元测试原理上抓不到」的 bug，见
[browser-verification.md](./browser-verification.md#一浏览器验证不可省)。

## M4：动效

已落地：GSAP Flip 布局过渡、惯性水滴形变、观测层挂起机制、演示区质感升级。

**演示区的 3D 方案已试过并放弃**，原因见 [pitfalls.md](./pitfalls.md#演示区的-3d-方案已试过并放弃)。
**演示区方块的视觉已定案为等距实体块**，动它之前先读 [pitfalls.md](./pitfalls.md#等距实体块)。

## M6 的 URL 分享（已提前落地并经浏览器验证）

落地的东西：`core/urlCodec.ts` 纯函数编解码、`composables/useShareUrl.ts` 防抖 300ms 写回地址栏、
`useFlexState` 首屏用 `decodeOrDefault(location.search)` 初始化。

浏览器实测四条：

1. 带短码的链接直接打开，能逐字段还原
2. 改属性后地址栏跟着变
3. 三次属性变化 `history.length` 纹丝不动（一律 `replaceState`）
4. A 标签页调出的链接在 B 标签页还原后，位置数值精确一致

两条不要踩反的时序约束，见 [pitfalls.md](./pitfalls.md#url-分享的两条时序约束)。

## 斜纹流向的重做（已实测通过）

原方案被浏览器实测推翻并重做，完整论证见
[pitfalls.md](./pitfalls.md#斜纹流向元素形状不能承担方向语义)。

实测通过的：

- `flex-start` 时行尾色块挂 `band-free--x-reverse`，切 `flex-end` 后色块移到行首并翻成 `x-forward`
- column 下相应翻成 `y-reverse` / `y-forward`
- 四份 pattern 都在 defs 里，两个 y 的带 `rotate(90)`，四条动画均在跑
- `@media (prefers-reduced-motion: reduce)` 的规则确认进了 CSSOM（解析结果 `animation-name: none`）

## M6：暗亮主题打磨（已完成）

站点默认暗色，亮色主题的问题一直被默认值盖住。把两套主题的变量代进 sRGB 插值与 WCAG 相对亮度
算了一遍，六处不达标——**没有一条是「看」出来的**：

| # | 缺陷 | 亮色 | 暗色 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | op-60 说明文字够不着 AA（站点八处这样用） | 4.11 | 6.33 | `--fg` 加深一档到 `#11151d`，一处管住八处 |
| 2 | 盒子描边糊在近白底上 | — | 3.68 | 描边强度拆成 `--stage-line-k`（亮 100% / 暗 55%）|
| 3 | 剩余空间斜纹基本看不见 | 1.46 | 2.20 | 不透明度拆成 `--stripe-op`（亮 0.65 / 暗 0.35）|
| 4 | 正面渐变翻成上暗下亮，与左上光源打架 | 0.591 → 0.697 | 正常 | 正面改从 `--face` 派生 |
| 5 | 方块接触阴影在台面上立不住 | 2.35 | 1.12 | 接触证据挪到方块底缘的 inset 暗边（暗 1.65 / 亮 3.03）|
| 6 | 选中态重写了一遍 background，与 `--face` 脱钩 | — | — | 删掉，三个面连同正面全部从 `--face` 派生 |

守卫测试 `src/styles/theme.spec.ts`（23 条）把这些判据钉住，两套主题各跑一遍。它自带算法自校验
（黑白 21:1、同色 1:1、对照 WCAG 文档的 `#767676` 在白底 4.54:1）——先证明算子没骗人，再谈结论。

**变异验证过**：`--fg` 退回 `#1f2430`，op-60 那条报 `expected 4.114467 to be >= 4.5`；
亮色 `--stripe-op` 退回 0.35，斜纹那条报 `expected 1.461983 to be >= 2`。数字与上表一致。

**测试只能算对比度与明暗序，观感算不出来**，仍归人眼——见下面的清单。
为什么这些参数必须分主题、为什么正面要从 `--face` 派生，见
[pitfalls.md](./pitfalls.md#暗亮主题六条只能算不能看的缺陷)。

## M7：布局改版（已完成，经浏览器实测）

改版的出发点：调属性要滚的是操作区，演示区不该因此被滚出视野。

落地的东西：

- 宽屏（≥lg）整页锁死不滚——`App.vue` 根上 `lg:h-full` + `lg:overflow-hidden`，
  左侧操作区 `lg:overflow-y-auto` 自己滚，右列演示区吃满剩余高度。
  **窄屏不锁**：两栏塌成一栏后一屏放不下，锁死等于把下半页永久切掉，所以每条都带 `lg:` 前缀。
- 整页宽度封顶从 1440（`max-w-360`）提到 1920（`max-w-480`），多出来的宽度全部落给演示区。
  演示容器自身的拖拽上限 `STAGE_LIMITS.maxWidth` 保持 1200 未动——可视宽已达 1526，
  原本拖到 1200 要横向滚才看得全的容器现在完整放得下，收益已经拿到，再提上限反而重新超出可视区。
- 明细表与 CSS 输出合并进新的 [`InspectorTabs.vue`](../src/components/playground/InspectorTabs.vue)，
  固定 208px 高、标签切换，默认停在明细（理论 vs 实际是本站的差异化核心，复制 CSS 是次要动作）。
  高度固定而不是自适应：行数随盒子增删变化，高度跟着跳会把演示区挤得忽大忽小。

浏览器实测（窗口 `visibilityState: hidden`，全程以 DOM 读数为准，一张截图没用；
测量通道先用已知值自校验过——剩余空间实测 456px，与 `720 − 3×80 − 2×12` 精确吻合）：

| 验的是什么 | 实测 |
| --- | --- |
| 整页真的不滚 | `scrollHeight === clientHeight`，`scrollTo(0,500)` 后 `scrollY` 仍为 0 |
| 没有静默裁切 | header 0~76、grid 76~907、两列 92~891、检视面板 683~891，底部恰好剩 16px 的 `p-4`，溢出视口 0px |
| 操作区自己滚 | `scrollHeight 981 > clientHeight 797`，`scrollTop` 能到 184 |
| 演示区可视范围 | 1526×521（原来约 1040 宽），容器拖到上限 1200 宽也完整放得下，不再横向滚 |
| 面板高度不随容器晃 | 容器 1200×600 与 600×120 两态下，演示面板恒 575、滚动容器恒 521 |
| 标签切换 | 切 CSS：代码出现、明细卸载、面板恒 208；切回明细：3 行俱在 |
| 顶面没被裁 | 滚动容器 `padding-top: 12px`，stage 顶边到内容顶实测 12 ≥ `blockDepth` 10 |

> ⚠️ `overflow: hidden` 会把溢出**藏起来**而不是报出来，所以「页面不滚」本身证明不了「装得下」。
> 结论是靠量每一层的 `top` / `bottom` 与视口边界的关系得出的，不是靠「没看见滚动条」。

排查中值得记一笔的假警报：演示区可视高先读到 577、后读到 521。真因是视口高从 963 变成 907
（窗口自己变了），`963 − 907 = 56` 与差值精确吻合，**不是布局随容器尺寸晃动**——
两种容器尺寸下各量一遍，面板高度恒定。

## 已由浏览器核对排除的三条

2026-09-18 用 DOM 与 computed style 核对（**不靠截图**：窗口 `visibilityState` 当时是 `hidden`，
截图拿到的是陈旧帧）。测量通道用已知理论值自校验过——默认状态剩余空间实测 456px，
与 `720 − 3×80 − 2×12` 精确吻合，说明布局读数可信。

- **column 下三盒宽度递减**——已排除。三盒均 720，`stretch` 正常。
  632 / 542 / 422 确认是冻帧假象，别再查。
- **wrap 换行时跨行的面互压**——不互压。默认 gap=12 下，第一行底边 154、第二行顶面顶点 156，余量 2px。
  `--d` 的公式正是为此设计的，见下面那条。
- **`align-items: stretch` 时顶面画到容器外**——**查出真缺陷并已修**：顶面上沿 2px 被外层
  `.overflow-auto` 裁掉（那层 padding 只有 8px，装不下 10px 的顶面）。已把 padding 提到 12px，
  `ThePlayground.spec.ts` 加了守卫钉住 `padding ≥ motion.blockDepth`。详见
  [pitfalls.md](./pitfalls.md#滚动容器的内边距必须容得下伸出去的面)。

### 顺带查清的两件事

**`--d` 的真实公式**是 `Math.max(3, Math.min(10, min(rowGap, columnGap) - 2))`，
带下限 3 和上限 10 的双向钳制，不是单纯的 `min(gap) - 2`。
`gap < 5` 时下限生效，厚度会大于间距：

| gap | 0 | 1 | 2 | 3 | 4 | ≥5 |
| --- | --- | --- | --- | --- | --- | --- |
| `--d` | 3 | 3 | 3 | 3 | 3 | gap−2 |
| 间距余量 | −3 | −2 | −1 | 0 | 1 | 2 |

**余量为负不是缺陷**。等距投影里靠下即靠前，第二行本就该遮住第一行的底部——
就像两块实体紧贴时前面那块会挡住后面那块。实测两行的 `.stage-box` 同为
`position: relative` + `z-index: auto` 且在同一层叠上下文，按 CSS 绘制顺序 DOM 靠后者在上，
第二行正好压在第一行上，**遮挡方向是对的**。
计划文档里「留 2px 余量」的措辞容易让人把这读成 bug 来"修"，实际它只影响
「两块看起来分不分离」，而 gap 小的时候它们本来就该看起来紧贴。

> ⚠️ 命中测试（`elementFromPoint`）验证不了这个：伪元素不参与命中测试，
> 探测点会穿透到下方的宿主元素上，读出来的归属是反的。只能按 CSS 绘制规则判断。

## 仍待人眼核对的清单

以下七条自动化量不出来——要么是观感，要么依赖改不了的系统开关与视口。

### 观感（纯主观，只能看）

1. 换成垂直 / 水平硬边条纹后，2.4s 周期会不会抢戏——硬边比 45° 斜纹更跳。
   **亮色主题下这条又加重一层**：`--stripe-op` 为追平可辨识度提到了 0.65（暗色 0.35），
   纹路更实，抢戏风险比暗色高。
2. 悬停时厚度加大与接触阴影拉开的手感，两者节奏是否一致（不一致会读成「先变厚再抬起」两段动作）。
3. `align-items: stretch` 时顶面伸到演示区上沿之外（现在不被裁了，但仍然在容器外），观感是否可接受。

### 动效（冻帧下测不了，读数必假）

4. 重排后面有没有残留。GSAP Flip 写的内联样式在冻帧下会留在中间帧，
   自动化读到的"残留"分不清是真残留还是冻结假象。
5. 系统打开「减少动态效果」后是否真的完全静止。CSSOM 规则已复验确实存在
   （`.stripe-flow → animation: none`，外加全局 `animation-duration: 0.01ms`），
   但**规则存在不等于运行时真停**。

### 视口（本机压不到）

6. 窄屏（<lg，1024px 以下）下两栏塌成一栏是否可读。M7 之后这里的行为变了：
   高度锁死与内部滚动全部只在 `lg:` 以上生效，窄屏退回普通文档流滚动，
   检视面板仍是固定 208px。本机 `resize_window` 不可控
   （三次请求 1100 / 720 / 500 分别得到 1280 / 1120 / 1920），**需要人眼拖窄窗口扫一遍**。

7. 标签面板 208px 的高度是否够用——三个盒子时明细表刚好放得下，
   盒子多了要在这块里滚；CSS 输出则一定要滚。这个取舍的观感只能看。

## 下一件事

M6 的两块（URL 分享、暗亮主题打磨）都已落地，i18n 已移出，M7 布局改版也已完成并实测，
两笔技术债都还清了：

> 已还清：状态 → CSS 的映射曾经散落多份，已统一到 `src/core/styleMap.ts`（`5adae3c`）。

代码侧没有排着队的活了，剩下的是：

- **人眼核对清单**（上面那节七条）——没有一条在真实窗口里过过。
  其中窄屏与 `prefers-reduced-motion` 两项受本机 `resize_window` 不可控所限，只能靠人。
- **部署**：站点尚未部署，README 里也还没有在线演示链接。

## 设计与计划文档

改动前务必先读对应的那份。

### 设计文档（specs）

- [2026-09-08-todo-flex-design.md](./superpowers/specs/2026-09-08-todo-flex-design.md)
  —— 定稿的总设计文档，含状态模型、模块划分、诊断层优先级、URL 短码格式、里程碑表
- [2026-09-09-stage-3d-motion-design.md](./superpowers/specs/2026-09-09-stage-3d-motion-design.md)
  —— 演示区 3D 方案（**已作废**，保留作决策记录）

### 实现计划（plans）

- [2026-09-08-core-engine-and-playground.md](./superpowers/plans/2026-09-08-core-engine-and-playground.md) —— M1 + M2
- [2026-09-09-overlay-and-resizer.md](./superpowers/plans/2026-09-09-overlay-and-resizer.md) —— M3 叠加层与 resize 手柄
- [2026-09-09-stage-3d-motion.md](./superpowers/plans/2026-09-09-stage-3d-motion.md) —— 3D 方案（**已作废**）
- [2026-09-09-isometric-solid-block.md](./superpowers/plans/2026-09-09-isometric-solid-block.md)
  —— 等距实体块，**开头的「三轮试错的结论」是动这块视觉前的必读**
