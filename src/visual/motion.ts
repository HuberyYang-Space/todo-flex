/**
 * 动效与视觉 token。
 *
 * 组件里禁止硬编码 duration / ease / 角度 / 像素上限——调参要能一处改完，
 * 也免得同一套节奏在各处漂移。CSS 里要用到的值由组件下发成自定义属性。
 */
export const motion = {
  /** 布局重排的时长（秒） */
  layoutDuration: 0.45,
  /** 布局重排的缓动：末端减速，收得干净 */
  layoutEase: 'power3.out',
  /** 按索引交错，让重排有节奏而不是齐步走（秒） */
  layoutStagger: 0.025,
  /** 悬停抬起的时长（秒） */
  liftDuration: 0.2,
  /** --depth 为 ±1 时的方块厚度（px） */
  maxDepth: 40,
  /** 悬停或选中时额外抬起的高度（px） */
  liftHeight: 18,
  /**
   * 演示区俯视角度（deg）。
   *
   * 立体面垂直于盒子平面，投影高度是 厚度 × sin(倾角)——10 度时只有 3px 左右，
   * 等于白做，所以取 22 度（投影约 15px，立体感读得出来）。
   *
   * rotateX 只在 Y-Z 平面内旋转，压缩的是纵向：默认的 row 方向主轴是横向，
   * 完全不受影响；透视带来的梯形畸变对所有盒子一致，横向比较依然公平。
   * 只有 column 方向的主轴会被压缩约 7%，读精确值仍以明细表为准。
   */
  tiltDeg: 22,
  /** 透视距离（px），越大越接近正交投影 */
  perspective: 1400,
} as const
